=Create=
git branch #list branches + curent branch
git branch new_feature #without checkout

cat .git/HEAD
ls  -la .git/refs/heads   #

=Swith =
git checkout BRANCH
cat .git/HEAD

git commit -am "Git second lesson" #Commit all with message

=Create with switch =
git checkout -b shorten_title 
git log --oneline

=swith with uncomited changes=
only  if  git can  resolve conflict  or changes are uncommitment

=Compare Branches=
git diff 7bf2624..HEAD
git diff --color-words 7bf2624..HEAD #One line changes
git diff --color-words 7bf2624..HEAD^

git branch --merged   #Brances merged into current
git branch --no-merged  #not merged

=Nename=